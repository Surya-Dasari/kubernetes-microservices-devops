pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build & Deploy SNAPSHOT') {
            steps {
                dir('services/user-service') {
                    sh 'mvn clean deploy'
                }
            }
        }
    }
}

